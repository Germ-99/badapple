# Bad Apple
Run bad apple right inside your terminal!

# Installing
First, please make sure you have the following dependencies:
- git
- ffmpeg
- chafa
- curl

**If you need the commands to download those**
```bash
# Debian/Ubuntu
sudo apt install git ffmpeg chafa curl

# Arch 
sudo pacman -S git ffmpeg chafa curl

# Fedora
sudo dnf install git ffmpeg chafa curl
```

## After you have dependencies
After installing the dependencies, run the following command:
```bash
curl -fsSL https://raw.githubusercontent.com/Germ-99/badapple/main/install.sh | bash
```

You're done!